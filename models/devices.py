# creating device object
class Device:
        def __init__(self,
                     diviceCategory,
                     deviceID,
                     deviceName,
                     deviceDescription,
                     deviceType):
                self.diviceCategory = diviceCategory
                self.deviceID = deviceID
                self.deviceName = deviceName
                self.deviceDescription = deviceDescription
                self.deviceType = deviceType
